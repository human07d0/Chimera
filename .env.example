# ============================
# Ops 运维界面鉴权（可选）
# 填写后，/ops 路由需要此密码才能访问
# 支持：停机、重启、运行时配置修改
# 留空则不启用运维界面
# ============================
OPS_PASSWORD=

# ============================
# 服务器配置
# ============================
PORT=3000
HOST=0.0.0.0

# ============================
# 上游 MiMo API 配置
# ============================
# 主代理使用（使用 token-plan 时可留空）
MIMO_API_KEY=
MIMO_BASE_URL=https://api.xiaomimimo.com
# Anthropic API Base URL（默认使用 MiMo 的 Anthropic 兼容接口）
ANTHROPIC_BASE_URL=https://api.xiaomimimo.com/anthropic/v1
# 启用的真实模型（逗号分隔）
# 可选：mimo-v2-flash / mimo-v2-pro / mimo-v2-omni / mimo-v2.5 / mimo-v2.5-pro
# 默认全开
MIMO_ENABLED_MODELS=mimo-v2-flash,mimo-v2-pro,mimo-v2-omni,mimo-v2.5,mimo-v2.5-pro

# ============================================
# Token-Plan 透传代理配置（可选）
# 启用后在主应用 /token-plan 路径下挂载透传路由
# 将请求原样转发到 token-plan 上游，不做虚拟模型映射
# ============================================
TOKEN_PLAN_ENABLED=false
# 客户端访问 token-plan 代理时的鉴权 Key（留空则不启用鉴权）
TOKEN_PLAN_PROXY_API_KEY=
# 上游 token-plan API Key（留空时回退到 MIMO_API_KEY）
TOKEN_PLAN_MIMO_API_KEY=
# 上游 OpenAI 格式 Base URL
TOKEN_PLAN_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
# 上游 Anthropic 格式 Base URL
TOKEN_PLAN_ANTHROPIC_BASE_URL=https://token-plan-cn.xiaomimimo.com/anthropic

UPSTREAM_TIMEOUT_MS=120000

# ============================
# 联网搜索默认配置（search 系列模型使用）
# ============================
WEB_SEARCH_MAX_KEYWORD=3
WEB_SEARCH_FORCE_SEARCH=true
# 单次搜索返回的最大网页数（越大 token 消耗越多）
WEB_SEARCH_LIMIT=3
WEB_SEARCH_COUNTRY=China
WEB_SEARCH_REGION=Beijing
WEB_SEARCH_CITY=Beijing

# ============================================
# 监控存储配置
# ============================================
# 可选值：memory / sqlite
MONITOR_STORAGE=sqlite
MONITOR_SQLITE_PATH=./data/monitor.db
MONITOR_RETENTION_DAYS=30
MONITOR_FLUSH_INTERVAL_MS=200
MONITOR_FLUSH_BATCH_SIZE=100
MONITOR_QUEUE_MAX_SIZE=10000

# ============================================
# 调试模式（可选）
# 开启后记录完整的请求/响应体到内存，提供 /debug Web UI
# ============================================
DEBUG_ENABLED=false
# 内存环形缓冲区最大记录数
DEBUG_MAX_RECORDS=500
# 单条记录最大 body 大小（字节），超过截断
DEBUG_MAX_BODY_SIZE=1048576

# ============================
# 日志级别: error | warn | info | debug
# ============================
LOG_LEVEL=info
